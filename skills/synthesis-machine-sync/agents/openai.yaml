interface:
  display_name: "Synthesis Machine Sync"
  short_description: "Enroll, sync, hand off, and retire fleet Macs"
  default_prompt: "Use $synthesis-machine-sync to enroll this Mac, sync shared state, hand work to another Mac, or retire a Mac from the fleet."

policy:
  allow_implicit_invocation: false
