interface:
  display_name: "Synthesis Preplan"
  short_description: "Apply the preplan synthesis workflow"
  default_prompt: "Use $synthesis-preplan to apply this workflow to the current task."

policy:
  allow_implicit_invocation: false
