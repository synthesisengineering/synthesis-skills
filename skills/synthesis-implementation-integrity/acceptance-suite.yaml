schema: 2
suite: item23-concurrent-seats-4-141-0
membership: closed
production_entry_point: All three client manifests and the top changelog
  identify 4.141.0, reporting version drift as PENDING under live
  install-plane claims and documenting the concurrent-seats ritual mode.
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and all-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: The fixtures execute release-contract, pending-parity,
  holder-rule, and manifest checks against fixtures and the working tree. They do not establish
  installed-client reload, live cross-client delivery, new-release publication,
  launcher refresh, or deployment approval. Those remain separate release
  checks. The ritual mode prose rides the release-metadata vehicle; review is the evidence
  of prose adoption. The full conformance and rituals suites run in the
  required pytest checks alongside this receipt.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .muse-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
- path: skills/synthesis-daily-rituals/SKILL.md
  cases:
  - release-version-metadata
- path: skills/synthesis-daily-rituals/references/version-history.md
  cases:
  - release-version-metadata
- path: skills/synthesis-agent-guardrails/tests/test_hook_gates.py
  cases:
  - hook-gate-catalog-hermetic
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-agent-conformance/scripts/conformance.py
  cases:
  - parity-pending-under-live-claim
- path: skills/synthesis-agent-conformance/scripts/test_conformance.py
  cases:
  - parity-pending-under-live-claim
- path: skills/synthesis-agent-conformance/scripts/test_parity.py
  cases:
  - parity-pending-under-live-claim
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: All client manifests and the top changelog identify the same release while retained
    release contracts remain valid. The mode prose rides this vehicle; review
    is the evidence of prose adoption.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: parity-pending-under-live-claim
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-agent-conformance/scripts/test_conformance.py::test_parity_reports_pending_under_live_install_claims
  motivating_defect: Parity checks failed while a release was in flight, and the ritual
    repaired the drift by refreshing clients under the live seat. Drift now reports
    PENDING with the holder named, and the ritual reports without repairing.
- id: hook-gate-catalog-hermetic
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-agent-guardrails/tests/test_hook_gates.py::test_hook_is_inert_when_unconfigured
  motivating_defect: The inert-hook test read the ambient catalog path, silent where a
    catalog exists and warning where it does not (4.140.0 CI). It now binds an
    empty fixture catalog before import, so all thirteen params pass anywhere.
