schema: 2
suite: immutable-runtime-installation-modes-4-101-1
membership: closed
production_entry_point: skills/synthesis-git-hooks/scripts/install.sh produces runtime files consumed by onboarding
  shared-runtime reconciliation
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and both-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: Physical fixtures execute the real installer, doctor and runtime reconciliation from readonly
  source in disposable homes. They do not establish new-release publication, installed-client reload, fresh native
  lifecycle receipts, live package channels or deployment approval. Those remain separate release acceptance checks.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
  - capabilities-match-runtime
- path: skills/synthesis-git-hooks/scripts/install.sh
  cases:
  - immutable-runtime-new-config
  - immutable-runtime-existing-config
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-onboarding/references/release-capabilities.json
  cases:
  - capabilities-match-runtime
- path: skills/synthesis-onboarding/scripts/test_runtime_payload.py
  cases:
  - immutable-runtime-new-config
  - immutable-runtime-existing-config
- path: skills/synthesis-onboarding/scripts/test_onboard.py
  cases:
  - sandbox-state-client-roots
  - additive-enrollment-preservation
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: Both client manifests and the top changelog identify the same patch release while retained
    release contracts remain valid.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: capabilities-match-runtime
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_check_capabilities.py::test_repository_capabilities_are_consistent
  motivating_defect: Published bootstrap release metadata agrees with plugin versions, source bytes and runtime
    capabilities.
- id: immutable-runtime-new-config
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_runtime_payload.py::test_actual_immutable_git_hooks_installer_reconciles_runtime_modes[new-config]
  motivating_defect: Actual readonly source copies must become canonical runtime modes and a writable newly seeded
    policy, remain repeatable, preserve source and refuse later destination mode drift.
- id: immutable-runtime-existing-config
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_runtime_payload.py::test_actual_immutable_git_hooks_installer_reconciles_runtime_modes[existing-config]
  motivating_defect: Runtime installation from readonly source must preserve existing custom policy bytes and permissions
    through repeated installation and reconciliation.
- id: sandbox-state-client-roots
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_onboard.py::SandboxIsolationTests::test_sandbox_binds_inherited_state_and_client_roots
  motivating_defect: A nested subprocess fixture must bind all selected state and client roots to its own home,
    preserve inherited foreign roots and use the real state writer without inherited path authority.
- id: additive-enrollment-preservation
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_additive_enrollment.py::test_real_engine_enroll_and_repair_preserve_personal_files_and_receipts
  motivating_defect: The real enrollment and repair engine must validate the fixture's selected home consistently
    while preserving personal files and receipts under inherited environment overrides.
