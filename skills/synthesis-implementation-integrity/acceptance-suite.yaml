schema: 2
suite: costume-effort-rationale
membership: closed
production_entry_point: scan_output.py flags cost-decided recommendations via
  the effort_rationale category, with quoted discussion exempted
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and both-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: Fixtures execute the real scanner in disposable
  directories. They do not establish installed-client reload, live hook pickup,
  new-release publication, or deployment approval. Those remain separate release checks.
changed_surfaces:
- path: skills/synthesis-anti-shortcuts/references/case-studies.md
  cases:
  - costume-effort-rationale
- path: skills/synthesis-anti-shortcuts/references/costume-vocabulary.md
  cases:
  - costume-effort-rationale
- path: skills/synthesis-anti-shortcuts/scripts/scan_output.py
  cases:
  - costume-effort-rationale
- path: skills/synthesis-anti-shortcuts/scripts/test_scan_output.py
  cases:
  - costume-effort-rationale
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
cases:
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: costume-effort-rationale
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-anti-shortcuts/scripts/test_scan_output.py::test_fraction_of_cost_hits_bare_usage
  motivating_defect: Cost must never decide a recommendation when the user
    optimizes for best; the scanner must catch the phrasing automatically.
