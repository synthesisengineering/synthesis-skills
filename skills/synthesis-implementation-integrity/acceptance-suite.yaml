schema: 2
suite: intake-56-retire-preserve-4-142-0
membership: closed
production_entry_point: All three client manifests and the top changelog
  identify 4.142.0, refusing worktree retirement while ignored files
  remain and closing intake 51 as resolved by 4.134.0.
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and all-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: The fixtures execute release-contract, retire-refusal,
  and manifest checks against fixtures and the working tree. They do not establish
  installed-client reload, live cross-client delivery, new-release publication,
  launcher refresh, or deployment approval. Those remain separate release
  checks. The full project-management suite runs in the
  required pytest checks alongside this receipt.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .muse-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-project-management/scripts/retire_worktree.py
  cases:
  - retire-refuses-ignored-content
- path: skills/synthesis-project-management/scripts/test_retire_worktree.py
  cases:
  - retire-refuses-ignored-content
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: All client manifests and the top changelog identify the same release while retained
    release contracts remain valid.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: retire-refuses-ignored-content
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-project-management/scripts/test_retire_worktree.py::test_refuses_ignored_only_worktree_and_names_files
  motivating_defect: Retirement checked plain porcelain status, so a worktree holding
    only ignored content read clean and took its ignored files with it on
    removal (intake 56, data-loss class). Every non-clean line of ignored
    status now refuses with its disposition recorded.
