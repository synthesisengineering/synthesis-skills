schema: 2
suite: snapshot-retry
membership: closed
production_entry_point: coordination.py retries board validation once when the
  claim-identity snapshot trips, accepting settled concurrent changes while
  persistent instability still fails loudly
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and both-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: Fixtures execute the real coordination validators in disposable
  directories. They do not establish installed-client reload, live cross-agent delivery,
  new-release publication, or deployment approval. Those remain separate release checks.
changed_surfaces:
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-project-management/scripts/coordination.py
  cases:
  - snapshot-retry
- path: skills/synthesis-project-management/scripts/test_claim_scope_cache.py
  cases:
  - snapshot-retry
cases:
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: snapshot-retry
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-project-management/scripts/test_claim_scope_cache.py::test_board_validation_accepts_settled_mid_check_change
  motivating_defect: A healthy-board advance must not be reported as corruption;
    one retry separates settled concurrent changes from real instability.
