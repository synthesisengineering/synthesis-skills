schema: 2
suite: project-resume-4-112-0
membership: closed
production_entry_point: All three client manifests and the top changelog
  identify 4.112.0, shipping the new open-source
  synthesis-project-resume skill v1.0.0 (one-prompt resume in any
  harness with session classification, the resume probe, and catalog
  registration) alongside its console companion surface
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and all-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: Fixtures execute the real probe, catalog audit,
  and manifest checks in disposable directories. They do not establish
  installed-client reload, live cross-client delivery, new-release
  publication, or deployment approval. Those remain separate release
  checks.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .muse-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-onboarding/references/components.json
  cases:
  - catalog-registered
- path: skills/synthesis-onboarding/references/release-capabilities.json
  cases:
  - catalog-registered
- path: skills/synthesis-project-resume/SKILL.md
  cases:
  - resume-probe
- path: skills/synthesis-project-resume/agents/openai.yaml
  cases:
  - resume-probe
- path: skills/synthesis-project-resume/references/requirements.md
  cases:
  - resume-probe
- path: skills/synthesis-project-resume/scripts/resume_probe.py
  cases:
  - resume-probe
- path: skills/synthesis-project-resume/scripts/test_resume_probe.py
  cases:
  - resume-probe
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: All client manifests and the top changelog identify the same release while retained
    release contracts remain valid.
- id: resume-probe
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-project-resume/scripts/test_resume_probe.py::test_probe_reports_identity_and_goal
  motivating_defect: A resume built on unread or misread project state
    strands the principal; the probe proves machine-readable identity,
    goal, session, format, and cross-machine changes.
- id: catalog-registered
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_onboard.py::WholeSystemTests::test_every_hard_stop_has_a_real_scaffold
  motivating_defect: A new skill invisible to the component catalog
    breaks the release gate and modular installs; the audit proves
    every live skill is cataloged.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
