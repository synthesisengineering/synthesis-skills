schema: 2
suite: pro-4-entrypoint-4-137-0
membership: closed
production_entry_point: All three client manifests and the top changelog
  identify 4.137.0, declaring the publish guard a verified-execution
  entrypoint and fixing the sites fixture git identity CI exposed.
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and all-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: The fixtures execute release-contract, entrypoint, fixture,
  and manifest checks against fixtures and the working tree. They do not establish
  installed-client reload, live cross-client delivery, new-release publication,
  launcher refresh, or deployment approval. Those remain separate release
  checks. The prose rides the release-metadata vehicle; review is the evidence
  of prose adoption. The full onboarding and guardrails suites run in the
  required pytest checks alongside this receipt.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .muse-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
- path: skills/synthesis-agent-guardrails/tests/test_publish_guard_sites.py
  cases:
  - entrypoint-sites-fixture-identity
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-onboarding/scripts/release_runtime.py
  cases:
  - entrypoint-publish-guard-declared
- path: skills/synthesis-onboarding/scripts/test_release_runtime.py
  cases:
  - entrypoint-publish-guard-declared
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: All client manifests and the top changelog identify the same release while retained
    release contracts remain valid. The entrypoint prose rides this vehicle; review
    is the evidence of prose adoption.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: entrypoint-publish-guard-declared
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-onboarding/scripts/test_release_runtime.py::test_declared_guard_entrypoint_executes_from_the_verified_release
  motivating_defect: The publish guard is a verified-execution entrypoint,
    so hook consumers run it from the verified active release instead of an
    unverified checkout path. Both guards execute through the same test.
- id: entrypoint-sites-fixture-identity
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-agent-guardrails/tests/test_publish_guard_sites.py::test_custom_roots_drive_the_mutation_scan
  motivating_defect: The sites fixture persists its git identity in the repo
    instead of relying on ambient global config, which CI does not provide.
