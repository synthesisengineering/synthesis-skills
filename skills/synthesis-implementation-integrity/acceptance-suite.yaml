schema: 2
suite: item-17-commit-gate-sync
membership: closed
production_entry_point: All three client manifests and the top changelog
  identify 4.135.0, shipping the commit-gate sync so every install
  re-runs the gate installer from the stable release pointer and the
  gate and the claim writer always agree.
enforcing_boundary: release.py consumes fresh schema-2 acceptance before publishing and all-client installation
receipt_consumer: synthesis-skills-manager.release.consume-acceptance.v1
change_base_policy: boundary-supplied-git-diff
expected_status: pass
metadata_class: acceptance-test
issues_authority_receipt: false
unverified_remainder: The fixtures execute release-contract, gate-sync, and
  manifest checks against fixtures and the working tree. They do not establish
  installed-client reload, live cross-client delivery, new-release publication,
  launcher refresh, or deployment approval. Those remain separate release
  checks. The SKILL.md prose rides the release-metadata vehicle; review is
  the evidence of prose adoption. The full release suite runs in the required
  pytest checks alongside this receipt.
changed_surfaces:
- path: .claude-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .codex-plugin/plugin.json
  cases:
  - release-version-metadata
- path: .muse-plugin/plugin.json
  cases:
  - release-version-metadata
- path: CHANGELOG.md
  cases:
  - release-version-metadata
- path: README.md
  cases:
  - release-version-metadata
- path: skills/synthesis-implementation-integrity/acceptance-suite.yaml
  cases:
  - manifest-validates
- path: skills/synthesis-skills-manager/scripts/release.py
  cases:
  - item17-gate-syncs-from-stable
  - item17-gate-sync-fails-closed
- path: skills/synthesis-skills-manager/scripts/test_release.py
  cases:
  - item17-gate-syncs-from-stable
  - item17-gate-sync-fails-closed
  - item17-gate-sync-reports-failure
cases:
- id: release-version-metadata
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_deduplicated_archive_release_contract_is_public_and_coherent
  motivating_defect: All client manifests and the top changelog identify the same release while retained
    release contracts remain valid. The gate-sync prose rides this vehicle; review is
    the evidence of prose adoption.
- id: manifest-validates
  control_class: acceptance-test
  expected_status: pass
  fixture: scripts/test_acceptance_suite.py::test_shipped_manifest_validates
  motivating_defect: The release gate consumes a closed exact changed-file universe with mapped fixture cases.
- id: item17-gate-syncs-from-stable
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_commit_gate_sync_runs_installer_from_stable_pointer
  motivating_defect: Item 17 — the release runs the gate installer from the
    stable pointer after repointing it at the verified install, so the gate
    can never again lag the claim writer.
- id: item17-gate-sync-fails-closed
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_commit_gate_sync_fails_closed_without_installer
  motivating_defect: Item 17 — a missing gate installer fails the release
    loudly instead of leaving a stale gate in place silently.
- id: item17-gate-sync-reports-failure
  control_class: acceptance-test
  expected_status: pass
  fixture: ../synthesis-skills-manager/scripts/test_release.py::test_commit_gate_sync_reports_installer_failure
  motivating_defect: Item 17 — a failing gate installer (its own doctor
    included) surfaces its receipt in the release record.
